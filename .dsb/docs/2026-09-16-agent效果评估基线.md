# Agent 效果评估 · 基线锚点(2026-09-16)

> 用途:用户将以本 Agent 在**其它项目**下执行任务,完成后回到本仓库统计、分析该 Agent 的实际工作效果。
> 本文件记录**起点**。回来时请新建「对比」章节(勿改写本基线段),用于配对分析。
>
> 🔴 **本文件含两个基线:v1(16:32:44,埋点前)与 v2(17:50:05,埋点齐备)。**
> **后续评估一律以文末「基线 v2」为准**;v1 仅作「turn_summary 埋点前」对照,不得改写。

## 一、时间锚点

| 项 | 值 |
|---|---|
| 本地时间 | **2026-09-16 16:32:44 CST**(Asia/Shanghai,星期三) |
| ISO 8601 | `2026-09-16T16:32:44+08:00` |
| UTC | `2026-09-16 08:32:44 UTC` |
| epoch(秒) | `1789547564` |

## 二、代码基线

| 项 | 值 |
|---|---|
| 仓库 | `/home/hange/projects/DSBAgent` |
| 版本 | `0.4.0` |
| HEAD | `b2e52af`(docs: 同步目标语义与裁剪策略调整) |
| 工作区 | 干净(无未提交改动) |
| 已装扩展 | `zhaoninghan.dsb-agent-0.4.0`,bundle md5 `312416295e347d00b64d54368da77ada`(= 本地构建) |
| 测试 | 112 文件 / 1228 tests 全绿;`tsc --noEmit` 干净 |

### 本轮已落地的关键机制变更(影响效果评估口径)

1. **6 段常驻任务地图已整块移除**(根因:六段均为压缩块 4 轨投影、零新增信息,
   且「已做/结果」段构成自我强化闭环 → 模型回应自身旁白而非推进任务。
   实测短消息 → LLM 轮次中位数 5 → 56 轮)。
2. **目标锚默认关闭**(`goalAnchorEnabled` 缺省 `false`)→ 地图与目标行**均不注入**。
3. **目标取值改「最新一条需求」**(原为需求轨首条,曾把「完成git处理」钉死数十天)。
4. **裁剪改为严格「只删块尾」**(规则 4 合规;修掉旧实现跨段取全局最新 seq 导致的前缀中部断裂)。
5. **块尾 hint 文案**改为「当前目标见任务锚」。

> ⚠️ **评估口径提醒**:以上变更(b2e52af 及前 3 个提交)均在 16:16 前已装,
> 本次基线包含它们。若要与「地图时代」效果对比,需注意基线已经历该修复。

## 三、统计基线(评估的原始数据来源)

统计落在 `~/.dsb/stats/<project>/events-YYYY-MM-DD.jsonl`(每项目一目录)。
**快照时刻各目录事件行数**:

| 项目目录 | 事件行数 | 文件数 | 备注 |
|---|---|---|---|
| `3d1dff85bb2f` | 3,785 | 5 | ≥08-19 |
| `3f5ca606a8c1` | 23,432 | 15 | 最大样本 |
| `default` | 1 | 2 | 空样本 |
| `home-hange-projects-deepseekagent` | 1,355 | 3 | 旧路径名(仓库改名前的遗留) |
| `mnt-share-dsbagent` | 411 | 2 | |
| **合计** | **28,984** | 27 | |

> 回来时的做法:重新枚举 `~/.dsb/stats/*/`,**新增目录 = 用户在其它项目的工作样本**;
> 对已有目录,增量 = 该目录新增行数。两者合并即为本次「跨项目使用」的原始事件集。

## 四、回来时建议的分析维度(占位,实施时按需细化)

- **规模**:跨了多少项目 / 多少会话 / 多少 round / 多少 tool 调用。
- **效率**:每轮任务的平均 round 数与工具调用数;是否存在"一个消息炸出几十个子任务"。
- **重复工作**:`tool_repeat` 埋点(重复调用同一工具同参数)占比 —— 直接对应历史上
  「输出被极端裁剪 → agent 重复劳动」这一担心。
- **信息丢失代价**:因 tool_result 裁剪/压缩导致的重试、回溯、重新读取文件的次数。
- **缓存**:稳定期命中率(基线 97.0~97.7%)、压缩后首轮(基线 49~54%);
  用 `scripts/analyze-cache-prefix.py` 对比。
- **失败模式**:工具调用报错率、REFUSED(瞬时标记拦截)、ROLLED BACK、上下文溢出等。

## 五、原始诉求留痕(用户原话要点)

> 「我会用这个 agent 到其它项目下做任务,最后我们回到这里来统计和分析这个 agent 的工作效果。」

---

# 基线 v2(2026-09-16 17:50:05 CST)★ 以此为准

> **为什么新增 v2**:v1(16:32:44)时 `turn_summary` 埋点**尚未落地**,故 v1 的样本
> **没有**「一次大任务 = 一条档案」的分母数据;且 v1 的 stats 目录仍受**旧 30 天裁剪**影响。
> v2 是**埋点齐备 + 保留期已放宽**后的干净起点 → **后续跨项目效果评估一律以 v2 为准**。
> (v1 保留不改写,用于「埋点前」对照。)

## 一、时间锚点

| 项 | 值 |
|---|---|
| 本地时间 | **2026-09-16 17:50:05 CST**(Asia/Shanghai,星期三) |
| ISO 8601 | `2026-09-16T17:50:05+08:00` |
| UTC | `2026-09-16 09:50:05 UTC` |
| epoch(秒) | `1789552205` |

## 二、代码基线

| 项 | 值 |
|---|---|
| 仓库 | `/home/hange/projects/DSBAgent` |
| 版本 | `0.4.0` |
| HEAD | `c61e0f6`(docs(stats): 轮次档案埋点设计说明 + CHANGELOG/project-overview 同步) |
| 工作区 | 干净(无未提交改动) |
| bundle md5 | `dd9ed9fb7f35fcf3f911a110f5cdf0dd`(本地 `dist/extension.js` 与**已装扩展逐字节一致**) |
| 已装扩展 | `zhaoninghan.dsb-agent-0.4.0`,安装于 17:11:54(晚于构建 17:11:45,顺序正确) |
| 测试 | **113 文件 / 1240 tests 全绿**;`tsc --noEmit` 干净;`lint:transient` 417 文件 0 污染 |

### v1 → v2 之间落地的变更(评估口径差异所在)

1. **新增 `turn_summary` 轮次档案埋点**(`f6c24be`):一次 send = 一条档案,含
   `rounds` / `toolCalls`(分母)/ `toolErrors` / `distinctTools` /
   `toolRepeatCount` / `toolRepeatWasteCount` / `redundantTokens` /
   `compactionCount` / `contextRecallCalls` / `appends` / 四项 token /
   `durationMs` / `chatMs` / `endReason`,并派生 `cacheHitRate`、`repeatWasteRate`。
   → **这是本次评估最关键的新分母数据**。
2. **`message_sent` 补 `sessionId`**(`b9a6d27`)。
3. **统计保留期可配置,默认 365 天**(`6c2dd72`,原硬编码 30 天)。
   → v1 之前被 30 天清掉的样本**无法找回**;v2 之后不再被清。

### 仍需留意的存量现象

- v2 时刻**已存在的会话**若在 v1 构建下开始,其**该会话内**不会补发 `turn_summary`(收尾在 finally,新构建加载后**新一轮 send** 才会落)。
- `turnIndex` 为**进程内**自增,重载 VS Code 后从 1 重计 → 跨会话统计请以 `sessionId` 分组。

## 三、统计基线(快照时刻)

| 项目目录 | 事件行数 | 文件数 | 覆盖区间 |
|---|---|---|---|
| `3d1dff85bb2f` | 4,075 | 4 | 2026-08-19 → 2026-09-16 |
| `3f5ca606a8c1` | 23,432 | 14 | 2026-08-11 → 2026-09-15 |
| `default` | 1 | 1 | 2026-08-11 |
| `home-hange-projects-deepseekagent` | 1,355 | 2 | 2026-08-09 → 2026-08-10 |
| `mnt-share-dsbagent` | 411 | 1 | 2026-08-15 |
| **合计** | **29,274** | **22** | |

`turn_summary` 在 v2 时刻条数 = **0**(埋点刚上线,尚无收尾样本)→ **v2 后的新档案即纯新增样本**。

> 回来时的做法:重新枚举 `~/.dsb/stats/*/`,**新增目录 = 用户在其它项目的工作样本**;
> 对已有目录,增量 = 该目录新增行数。`turn_summary` 条数即为「大任务」总数。

## 四、回来时建议的分析维度(占位,实施时按需细化)

- **规模**:跨了多少项目 / 多少会话 / 多少 `turn_summary`(大任务)/ 多少 round / 多少 tool 调用。
- **效率**:`rounds` 与 `toolCalls` 的分布(中位数/P90);是否存在"一个消息炸出几十个子任务"。
- **重复工作**:`toolRepeatWasteCount / toolCalls`(= `repeatWasteRate`)—— 直接对应历史上
  「输出被极端裁剪 → agent 重复劳动」这一担心(此前**只有分子没有分母**)。
- **信息丢失代价**:`contextRecallCalls` / `turn_summary` —— 信息丢失的直接计分板。
- **缓存**:`cacheHitRate` 分布(权威口径 `cacheRead/(cacheRead+input)`);
  稳定期与压缩后首轮分别用 `scripts/analyze-cache-prefix.py` 对比。
- **失败模式**:`endReason` 分布(`maxRounds`/`aborted`/`error` 占比)+ `toolErrors`;
  另统计 REFUSED(瞬时标记拦截)、ROLLED BACK、上下文溢出等。

## 五、原始诉求留痕(用户原话要点)

> 「我会用这个 agent 到其它项目下做任务,最后我们回到这里来统计和分析这个 agent 的工作效果。」
>
> 「记录一下时间,这是后续统计分析的基线时间」(2026-09-16 17:50:05 CST)
